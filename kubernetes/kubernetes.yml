apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-ma
  namespace: default
spec:
  selector:
    matchLabels:
      app: react-ma
  template:
    metadata:
      labels:
        app: react-ma
    spec:
      containers:
        - name: react-ma
          image: react-ma:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              port: 8080
              path: "/api/health"
            initialDelaySeconds: 15
            timeoutSeconds: 1
          readinessProbe:
            httpGet:
              port: 8080
              path: "/api/health"
            initialDelaySeconds: 15
            timeoutSeconds: 1
          startupProbe:
            httpGet:
              port: 8080
              path: "/api/health"
            initialDelaySeconds: 15
            timeoutSeconds: 1
          resources:
            limits:
              cpu: "1"
              memory: "500Mi"
            requests:
              cpu: "1"
              memory: "500Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: react-ma
  namespace: default
spec:
  selector:
    app: react-ma
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: NodePort

---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: react-ma
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: react-ma
  minReplicas: 3
  maxReplicas: 9
  targetCPUUtilizationPercentage: 70
